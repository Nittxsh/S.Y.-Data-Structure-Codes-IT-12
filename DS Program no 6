Program 6: Write programs for the implementation of graph traversals by applying BFS.

Algorithm for BFS: 

Algorithm: BFS(G, start)
Input: 
    G = (V, E)  → graph represented by adjacency matrix or adjacency list
    start       → starting vertex

Output: 
    All vertices reachable from 'start' in BFS order

Steps:
1. Initialize an array visited[V] ← 0  // mark all vertices as unvisited
2. Create an empty queue Q
3. Enqueue(start)
4. Mark visited[start] ← 1
5. Repeat while Q is not empty:
       a. v ← Dequeue(Q)
       b. Process v (print v)
       c. For each vertex u adjacent to v: 
              If visited[u] = 0 then
                   Enqueue(u)
                   visited[u] ← 1
6. End While

#include <stdio.h>
#define MAX 10

int adj[MAX][MAX];
int visited[MAX];
int queue[MAX];
int front = -1, rear = -1;
int n;  // number of vertices

void enqueue(int v) {
    if (rear == MAX - 1) return;
    if (front == -1) front = 0;
    queue[++rear] = v;
}

int dequeue() {
    if (front == -1 || front > rear) return -1;
    return queue[front++];
}

void BFS(int start) {
    printf("\n>>> BFS Traversal Output\n");
    printf("Visited Order => ");

    enqueue(start);
    visited[start] = 1;

    int v;
    while ((v = dequeue()) != -1) {
        printf("%d ", v);

        for (int i = 0; i < n; i++) {
            if (adj[v][i] == 1 && visited[i] == 0) {
                enqueue(i);
                visited[i] = 1;
            }
        }
    }

    printf("\n");
}

int main() {
    int start;

    printf("=== BFS on Graph (Adjacency Matrix) ===\n");
    printf("Enter number of vertices: ");
    scanf("%d", &n);

    printf("\nEnter adjacency matrix (%d x %d):\n", n, n);
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            scanf("%d", &adj[i][j]);

    printf("\nEnter starting vertex (0 to %d): ", n - 1);
    scanf("%d", &start);

    for (int i = 0; i < n; i++) visited[i] = 0;

    BFS(start);

    printf("\nProcess completed.\n");
    return 0;
}

Output:

=== BFS on Graph (Adjacency Matrix) ===
Enter number of vertices: 4

Enter adjacency matrix (4 x 4):
1 1 0 0
1 1 1 0
0 1 1 1
0 0 1 1

Enter starting vertex (0 to 3): 0

>>> BFS Traversal Output
Visited Order => 0 1 2 3 

Process completed.
